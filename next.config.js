/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["placehold.co", "www.lovethispic.com", "source.unsplash.com"],
  },
};

module.exports = nextConfig;
